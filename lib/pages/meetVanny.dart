import 'package:flutter/material.dart';

class MeetVanny extends StatelessWidget {
  static const String routeName = "/meetVanny";
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
