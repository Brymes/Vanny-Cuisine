import 'package:flutter/material.dart';

class PreviousOrders extends StatelessWidget {
  static const String routeName = "/recent";
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
